import { Box, Flex } from '@chakra-ui/react';
import React from 'react';

const Footer = ({}) => {
  return (
    <Flex h="10vh" mt="auto">
      <Box>Footer</Box>
    </Flex>
  );
};

export default Footer;
